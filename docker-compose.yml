services:
  payment-api:
    build: .
    container_name: vulnerable-payment-api
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    volumes:
      - ./:/app
    restart: unless-stopped
